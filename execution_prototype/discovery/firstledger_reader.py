from typing import List, Dict, Any
from execution_prototype.discovery.models import RawDiscoveryEvent

def parse_firstledger_fixture(data: List[Dict[str, Any]]) -> List[RawDiscoveryEvent]:
    # Placeholder for optional FirstLedger integration
    # Must be read-only and deterministic
    events = []
    return events
